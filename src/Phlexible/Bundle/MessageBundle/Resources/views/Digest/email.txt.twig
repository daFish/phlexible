Digest Mail
Messages sind {{ digest.lastSend|date }}

{% for message in digest.messages %}
===============================================================================
{{ message.user|default("?") }}, {{ message.createdAt|date }} [{{ message.type|default("-") }}, {{ message.channel|default("-") }}, {{ message.role|default("-") }}]
-------------------------------------------------------------------------------
{{ message.subject }}
{% if message.body %}
-------------------------------------------------------------------------------
{{ message.body }}
{% endif %}
{% endfor %}
